{
  "name": "photo-game",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build:met-catalog": "node scripts/build-met-catalog.js",
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
